# Nix build results
result
result-*

# direnv
.direnv/

# Terraform
.terraform/
terraform.tfstate
terraform.tfstate.*.backup
terraform.tfvars
.terraform.lock.hcl
*.tfplan

# Runtime state — these belong in deployment repos (amon-sul, tunnel),
# never in the cococoir source tree.
sops/
vars/
inventory.json
