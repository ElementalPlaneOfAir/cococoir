# yaml-language-server: $schema=https://devenv.sh/devenv.schema.json
inputs:
  nixpkgs:
    url: github:cachix/devenv-nixpkgs/rolling
  # Same rust-overlay rev the cococoir flake pins, so the dev shell's
  # toolchain and the nix build's toolchain can never diverge.
  rust-overlay:
    url: github:oxalica/rust-overlay/26a71e661c47bd21a05d06fec749f3f7c75e9d12
    flake: false

# If you're using non-OSS software, you can set allow_unfree to true.
# allow_unfree: true

# If you're not willing to allow unsupported packages:
# allow_unsupported_system: false

# If you're willing to use a package that's vulnerable
# permitted_insecure_packages:
#  - "openssl-1.1.1w"

# If you have more than one devenv you can merge them
#imports:
# - ./backend
